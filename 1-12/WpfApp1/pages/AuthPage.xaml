<Page x:Class="WpfApp1.Pages.AuthPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Авторизация">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Логин -->
        <Label Content="Логин:" Grid.Row="0" VerticalAlignment="Center"/>
        <TextBox x:Name="TextBoxLogin" Grid.Row="0" Margin="100,0,0,0" Width="200" 
                 Background="Transparent" ToolTip="Введите ваш логин"/>

        <!-- Пароль -->
        <Label Content="Пароль:" Grid.Row="1" VerticalAlignment="Center"/>
        <PasswordBox x:Name="PasswordBoxPassword" Grid.Row="1" Margin="100,0,0,0" Width="200"
                     ToolTip="Введите ваш пароль"/>

        <!-- Кнопки -->
        <Button Content="Вход" Grid.Row="2" Margin="0,10,0,0" Width="100" 
                HorizontalAlignment="Left" Click="LoginButton_Click"/>

        <Button Content="Регистрация" Grid.Row="2" Margin="120,10,0,0" Width="150"
        HorizontalAlignment="Left" Click="RegisterButton_Click"/>
    </Grid>
</Page>
